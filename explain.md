# SkillCampus 프로젝트 구조 설명서

이 문서는 동료 개발자가 프로젝트를 빠르게 파악하고 협업할 수 있도록 현재 남아있는 폴더와 파일들의 역할 및 구조를 설명합니다.

---

## 📂 최상위 (Root) 디렉토리

프로젝트의 전반적인 환경 설정 및 패키지 정보를 담고 있습니다.

- **`.env.example`**
  - 환경 변수 템플릿 파일입니다. `.env.local`에 들어갈 `SUPABASE_URL`, `SUPABASE_ANON_KEY`, `GEMINI_API_KEY` 등의 구조를 동료에게 알려주기 위해 만들어졌습니다.
- **`database.sql`**
  - Supabase 데이터베이스 세팅을 위한 SQL 쿼리문과 보안 규칙(RLS 정책)들이 모두 담겨 있습니다. 이 코드를 복사해 Supabase에서 실행해야 백엔드 연동이 완료됩니다.
- **`package.json` / `package-lock.json`**
  - 프로젝트에 설치된 외부 라이브러리(Next.js, Tailwind, Supabase 등)의 목록과 버전 정보, 실행 명령어(`npm run dev`)가 기록된 파일입니다.
- **`tsconfig.json`**
  - TypeScript 컴파일러 설정 파일입니다.
- **`next.config.ts` / `eslint.config.mjs` / `postcss.config.mjs`**
  - 각각 Next.js 설정, 코드 포맷팅/오류 검사(ESLint) 설정, CSS 후처리(Tailwind 연동용) 설정 파일입니다.
- **`.gitignore`**
  - 깃허브에 올라가면 안 되는 파일(용량이 큰 `node_modules`, 보안상 중요한 `.env` 등)의 목록이 적혀 있습니다.

---

## 📂 src 디렉토리 (주요 소스 코드)

실제 웹사이트의 기능과 화면을 구성하는 핵심 코드들이 모여 있습니다.

### 1. `src/app/` (Next.js App Router 화면 구성)
페이지(URL 라우팅) 단위로 폴더가 나뉘어져 있습니다.

- **`page.tsx` / `layout.tsx` / `globals.css`**
  - 메인 홈 화면(대시보드), 전체 레이아웃 구조(네비게이션 바 포함), 그리고 전역 디자인(Tailwind CSS) 설정입니다.
- **`auth/`**
  - 회원가입 및 로그인 페이지 화면과 기능을 담당합니다.
- **`board/`**
  - 4개의 주요 게시판 (자유, 스킬교환, 공모전, 스터디)의 글 목록, 글쓰기, 상세 보기 화면을 담당합니다.
- **`chat/`**
  - 실시간 1:1 통신을 지원하는 채팅방 화면이며, 교환 완료 시 평가를 남기는 리뷰 모달 기능도 포함되어 있습니다.
- **`profile/`**
  - 사용자의 내 정보 보기, 스킬 진행도 확인, AI 요약 강점 및 리뷰 이력을 보여주는 프로필 상세 페이지입니다.

### 2. `src/components/` (공통 UI 컴포넌트)
여러 페이지에서 재사용되는 버튼, 네비게이션, 모달 등의 UI 조각들을 모아둔 곳입니다.

- **`layout/`**
  - 페이지 하단/상단 네비게이션 바(Bottom Nav, Header 등)를 구성하는 컴포넌트입니다.
- **`theme-provider.tsx`**
  - 다크모드/라이트모드 등 전반적인 웹사이트 테마를 적용하고 관리하는 설정입니다.

### 3. `src/lib/` (데이터 연동 및 유틸리티 함수)
데이터베이스 통신, 상태 관리 등 화면에 보이지 않는 로직(기능)들을 처리합니다.

- **`supabase/`**
  - Supabase 백엔드와 연결하기 위한 클라이언트 초기화 코드가 있습니다.
- **`useAuth.ts`**
  - 현재 로그인한 사용자의 상태(세션)를 관리하고, 회원가입/로그인 로직을 처리하는 커스텀 훅(Hook)입니다.
- **`usePosts.ts`**
  - 게시판의 글을 불러오고, 쓰고, 지우고, 수정하는 (CRUD) 데이터 연동을 담당하는 커스텀 훅입니다.
- **`avatar.ts`**
  - 사용자 아이디(seed)를 바탕으로 프로필 아바타 이미지를 랜덤하게 생성해주는 기능입니다.
- **`utils.ts`**
  - 주로 Tailwind CSS의 클래스명들을 조건부로 예쁘게 합쳐주는 공통 함수(`cn`)가 들어 있습니다.

---

## 💡 동료 개발자를 위한 참고사항
이 레포지토리를 클론(Clone) 받은 후 아래 순서로 진행하면 바로 로컬 환경에서 사이트를 띄울 수 있습니다.

1. 터미널에서 `npm install` 실행 (패키지 설치)
2. `.env.example` 파일을 복사하여 `.env.local`로 이름 변경 후 Supabase Key 입력
3. Supabase SQL Editor에 `database.sql` 내용 전체 실행 (DB 테이블 및 권한 생성)
4. `npm run dev` 실행하여 개발 서버 켜기
